import styled from "styled-components";

export const Container = styled.main`
   position: relative;
   margin: 5rem auto 0;
   width: 40%;
   min-width: 50rem;
   display: flex;
   align-items: center;
   justify-content: center;
`