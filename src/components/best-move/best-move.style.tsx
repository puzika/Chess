import styled from "styled-components";
import * as svar from '../../variables.style';

export const BestMoveContainer = styled.div`
   background-color: ${svar.clrPrimary};
   padding: 1rem 0;
   border-radius: inherit;
`;

export const BestMove = styled.p`
   font-size: 2.5rem;
   text-align: center;
`;